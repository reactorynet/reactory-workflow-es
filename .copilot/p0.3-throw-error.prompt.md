You are working in the reactory-workflow-es monorepo (a fork of workflow-es).
Conventions: one class per file (kebab-case); ExecutionResult via static factories; Jasmine specs
live under core/spec.

TASK (UPGRADES.md P0.3): Bare string `throw` statements lose stack traces and make the
`catch (err) { err.message }` blocks read `undefined`. Replace them with real `Error` objects.

SCOPE (source): core/src/**/*.ts. There are exactly two string throws today:
  - core/src/services/workflow-executor.ts:31
      throw "No workflow definition in registry for " + instance.workflowDefinitionId + ":" + instance.version;
  - core/src/fluent-builders/step-builder.ts:110
      throw "Parent step of name " + stepName + " not found";
Grep the whole of core/src for any other `throw "` or throw of a template/string literal and convert
those too.

CHANGE: convert each to `throw new Error(\`...\`)` using a template literal, preserving the exact
message text. Do NOT change control flow or anything else.

TEST (add, TDD): create core/spec/services/throw-error.spec.ts with a Jasmine `describe` that
asserts the step-builder `.end()` path throws an `Error` instance (not a string) when the named
parent step does not exist. Build a minimal WorkflowBuilder<any>, start a step, and call
`.end("does-not-exist")` inside an `expect(() => ...).toThrowError()`. Keep it synchronous (no async/
done). Look at an existing spec under core/spec for the correct import paths from "../../src".

DO NOT: touch core/build, node_modules, providers, or files outside core/. Run yarn ONLY inside the
core/ directory — never at the repo root (it pollutes the root with package.json/.yarnrc.yml/.yarn).
Do not run `npm pack` / `build:pack`; do not modify any *.tgz.

VERIFY: `cd core && yarn build && yarn test`. Report the final "N specs, M failures" line; it must be
0 failures and the new spec must be included (count goes up).
