You are working in the reactory-workflow-es monorepo (a fork of workflow-es).
Conventions: one class/util per file (kebab-case); barrels re-export via core/src/abstractions.ts.

CONTEXT: We are preparing to enable TypeScript `strict` (which turns on
`useUnknownInCatchVariables`, making `catch (err)` give `err: unknown`). Today every `catch (err)`
block treats `err` as `any` — reading `err.message`/`err.stack` and concatenating `"..." + err`.
Both break under strict. We fix this now, but the build still uses the CURRENT tsconfig
(noImplicitAny only) — your changes MUST compile under the current config too.

TASK (UPGRADES.md P1.1): Introduce a `toError(unknown): Error` helper and route every catch block
through it.

STEP 1 — create core/src/abstractions/errors.ts:
    /** Normalises an unknown thrown value into an Error (strict-catch safe). */
    export const toError = (e: unknown): Error => (e instanceof Error ? e : new Error(String(e)));

STEP 2 — add to the barrel core/src/abstractions.ts (append a line):
    export * from "./abstractions/errors";

STEP 3 — update EVERY catch block in these files. In each `catch (err) { ... }`, add as the FIRST
line `const error = toError(err);` and replace all uses of `err` inside that block with `error`
(so `err.message` -> `error.message`, `err.stack` -> `error.stack`, `"..." + err` -> `"..." + error.message`,
and a bare `err` passed to logger -> `error`). Import `toError` from "../abstractions" at the top of
each file (it is exported from the abstractions barrel). The files and catch sites:
    core/src/services/poll-worker.ts            (2 catch blocks: ~L43, ~L53)
    core/src/services/event-queue-worker.ts     (3: ~L49, ~L81, ~L102)
    core/src/services/workflow-executor.ts      (1: ~L85 — also uses err.message/err.stack at ~L88,99,100)
    core/src/services/workflow-host.ts          (3: ~L114, ~L141, ~L166)
    core/src/services/workflow-queue-worker.ts  (2: ~L49, ~L94)
  In workflow-executor.ts specifically: `perr.message = err.message` -> `error.message`;
  `message: err.message || String(err)` -> `message: error.message`; `stack: err.stack || null` ->
  `stack: error.stack || null`.
  Preserve all surrounding logic, log message text, and control flow EXACTLY — only normalise err.

STEP 4 — add a unit test core/spec/services/to-error.spec.ts (synchronous Jasmine, no async/done):
    - toError(new Error("x")) returns the SAME Error instance unchanged.
    - toError("boom") returns an Error whose .message === "boom".
    - toError({code: 1}) returns an Error instance (message is a string).
  Import `toError` from "../../src".

DO NOT: touch core/build, node_modules, providers, or change tsconfig (strict is a later item). Run
yarn ONLY inside core/ — never at the repo root. Do not run npm pack / build:pack; do not modify any
*.tgz.

VERIFY: `cd core && yarn build && yarn test`. Report the final "N specs, M failures" line — must be
0 failures and the spec count must increase by the new tests.
