You are working in the reactory-workflow-es monorepo (a fork of workflow-es).

TASK (UPGRADES.md P0.0): The Jasmine test suite is red. Jasmine 5 forbids a spec
function that is BOTH declared `async` AND takes a `done` callback. The scenario specs use
`beforeAll(async (done) => { ... spinWaitCallback(fn, done); })`, which throws
"An asynchronous before/it/after function was defined with the async keyword but also took a
done callback."

A promise-based helper already exists in `core/spec/helpers/spin-wait.ts`:
`export function spinWait(until: () => Promise<boolean>): Promise<void>`. Use it.

SCOPE: only these files —
  core/spec/scenarios/basic-workflow.spec.ts
  core/spec/scenarios/data-io.spec.ts
  core/spec/scenarios/external-events.spec.ts
  core/spec/scenarios/if.spec.ts
  core/spec/scenarios/outcome-fork.spec.ts
  core/spec/scenarios/parallel.spec.ts
  core/spec/scenarios/while.spec.ts
  core/spec/services/execution-result-processor.spec.ts

CHANGE, in each scenario spec:
  1. Change every `beforeAll(async (done) => {` to `beforeAll(async () => {` (also any
     `beforeEach`/`afterAll`/`it` that uses the `async (done)` form).
  2. Replace each `spinWaitCallback(<fn>, done);` call with `await spinWait(<fn>);`.
  3. Update the import: import `spinWait` from `../helpers/spin-wait` instead of (or in
     addition to) `spinWaitCallback`. Remove `spinWaitCallback` from the import if it is no
     longer used in that file.
  4. Do NOT change the `until` callback bodies or any assertions — only the async/done wiring.
For `core/spec/services/execution-result-processor.spec.ts`: it is synchronous and does not use
  `spinWaitCallback` — simply remove the now-unused `import { spinWaitCallback } from ...` line.

DO NOT: touch core/src, core/build, node_modules, providers, the spin-wait.ts helper itself,
or any file outside the scope list. Do not change test logic or timeouts.

VERIFY: run `cd core && yarn build && yarn test` — the build must pass and the test run must
report 0 failures. Report the exact final "N specs, M failures" line.
